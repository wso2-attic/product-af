<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <groupId>${groupId}</groupId>
    <artifactId>${rootArtifactId}-main_car_artifact</artifactId>
    <version>${version}</version>
    <packaging>pom</packaging>
    <name>${rootArtifactId}-main_car_artifact</name>
    <description>${rootArtifactId}-main_car_artifact</description>
    <modelVersion>4.0.0</modelVersion>
    <dependencies>
      <dependency> 
        <groupId>ant</groupId> 
        <artifactId>ant-optional</artifactId> 
        <version>1.5.3-1</version> 
      </dependency> 
      <dependency>
        <groupId>org.apache.ant</groupId>
        <artifactId>ant</artifactId>
        <version>1.9.2</version>
      </dependency>
      <dependency>
        <groupId>org.apache.ant</groupId>
        <artifactId>ant-launcher</artifactId>
        <version>1.9.2</version>
      </dependency>
    </dependencies>
    <build>
        <plugins>
          <plugin>
            <artifactId>maven-assembly-plugin</artifactId>
            <version>2.4</version>
            <executions>
              <execution>
                <id>2-dist</id>
                <phase>package</phase>
                <goals>
                  <goal>attached</goal>
                </goals>
                <configuration>
                  <descriptors>
                    <descriptor>bin.xml</descriptor>
                  </descriptors>
                  <appendAssemblyId>false</appendAssemblyId>
                  <outputDirectory>../../../${rootArtifactId}_deploy_artifact</outputDirectory>
                  <finalName>${rootArtifactId}_${version}</finalName>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
           <groupId>org.apache.maven.plugins</groupId>
           <artifactId>maven-antrun-plugin</artifactId>
 	   <version>1.7</version>
           <executions>
             <execution>
               <phase>install</phase>
                 <configuration>
                   <target>
                     <move file="../../../${rootArtifactId}_deploy_artifact/${rootArtifactId}_${version}.zip"
                       tofile="../../../${rootArtifactId}_deploy_artifact/${rootArtifactId}_${version}.car" />
                   </target>
                 </configuration>
                 <goals>
                  <goal>run</goal>
                 </goals>
               </execution>
             </executions>
         </plugin>
        </plugins>
    </build>
</project>

